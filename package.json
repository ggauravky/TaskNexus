{
  "name": "tasknexus",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "verify": "npm --prefix backend run lint && npm --prefix backend run validate:migrations && npm --prefix backend test && npm --prefix frontend run lint && npm --prefix frontend run build"
  }
}
