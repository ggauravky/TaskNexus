{
  "name": "tasknexus",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "verify": "npm --prefix frontend run lint && npm --prefix frontend run build && npm --prefix backend test"
  }
}
